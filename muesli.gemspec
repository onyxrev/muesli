Gem::Specification.new do |s|
  s.name         = 'muesli'
  s.version      = '0.0.4'
  s.date         = '2014-12-26'
  s.summary      = "Provides class-based recursive serialization of models into hashes with attribute whitelisting and conditionals for passing to views or as an API response."
  s.description  = "Provides class-based recursive serialization of models into hashes with attribute whitelisting and conditionals for passing to views or as an API response."
  s.authors      = ["Dan Connor"]
  s.email        = 'danconn@danconnor.com'
  s.files        = Dir["{lib}/**/**/*"]
  s.require_path = "lib"
  s.homepage     = 'https://github.com/onyxrev/muesli'
  s.license      = 'MIT'
end
